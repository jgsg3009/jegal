export var buildIterator = function buildIterator(list) {
  return {
    index: 0,
    getNext: function getNext() {
      return list[this.index++];
    },
    reset: function reset() {
      this.index = 0;
    }
  };
};